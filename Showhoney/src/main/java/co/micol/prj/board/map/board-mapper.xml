<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="co.micol.prj.board.serviceImpl.BoardMapper">

	<select id="getSelectList" resultType="co.micol.prj.board.vo.BoardVo">
		SELECT * FROM BOARD ORDER BY BOARDID DESC
	</select>
	
	<select id="getSelect" parameterType="co.micol.prj.board.vo.BoardVo" resultType="co.micol.prj.board.vo.BoardVo">
		SELECT * FROM BOARD WHERE BOARDID = #{boardId}
	</select>
	
	<insert id="boardInsert" parameterType="co.micol.prj.board.vo.BoardVo">
		
	</insert>
	
	<update id="boardUpdate" parameterType="co.micol.prj.board.vo.BoardVo">
	
	</update>
	
	<delete id="boardDelete" parameterType="co.micol.prj.board.vo.BoardVo">
		
	</delete>
	
</mapper>